services:
  gap-library-front:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000/api/v1}
    image: gap-library-front:latest
    ports:
      - "80:80"
    restart: unless-stopped
