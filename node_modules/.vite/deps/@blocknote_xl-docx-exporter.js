import {
  AlignmentType,
  CheckBox,
  ExternalHyperlink,
  File,
  ImageRun,
  LevelFormat,
  Packer,
  PageBreak,
  Paragraph,
  ShadingType,
  Tab,
  Table,
  TableCell,
  TableRow,
  TextRun
} from "./chunk-FSNW4YWI.js";
import {
  G,
  Ni,
  T,
  x
} from "./chunk-VEWEF43G.js";
import "./chunk-5NNLBS7K.js";
import "./chunk-2TUXWMP5.js";

// node_modules/@blocknote/xl-docx-exporter/dist/blocknote-xl-docx-exporter.js
async function y(e) {
  if (typeof window < "u") {
    let t = await createImageBitmap(e), { width: n, height: r } = t;
    return t.close(), {
      width: n,
      height: r
    };
  } else {
    let t = (await import("./dist-2DQM3HD3.js")).imageMeta, n = t(new Uint8Array(await e.arrayBuffer()));
    if (!n.width || !n.height) throw Error("Image dimensions not found");
    return {
      width: n.width,
      height: n.height
    };
  }
}
var b = (e, t) => {
  let i = Array(e.headerRows ?? 0).fill(true), a = Array(e.headerCols ?? 0).fill(true);
  return new Table({
    layout: "autofit",
    columnWidths: e.columnWidths.map((e2) => (e2 ?? 120) * 0.75 * 20),
    rows: e.rows.map((o, s) => {
      let c = i[s];
      return new TableRow({
        tableHeader: c,
        children: o.cells.map((i2, o2) => {
          var _a;
          let s2 = (_a = e.columnWidths) == null ? void 0 : _a[o2], l = x(i2), u = a[o2];
          return new TableCell({
            width: s2 ? {
              size: s2 * 0.75 * 20,
              type: "dxa"
            } : void 0,
            columnSpan: l.props.colspan,
            rowSpan: l.props.rowspan,
            shading: l.props.backgroundColor === "default" || !l.props.backgroundColor ? void 0 : {
              type: ShadingType.SOLID,
              color: (() => {
                var _a2;
                let e2 = (_a2 = t.options.colors[l.props.backgroundColor]) == null ? void 0 : _a2.background;
                if (e2) return e2.slice(1);
              })()
            },
            children: [new Paragraph({
              children: t.transformInlineContent(l.content),
              alignment: !l.props.textAlignment || l.props.textAlignment === "left" ? void 0 : l.props.textAlignment === "center" ? "center" : l.props.textAlignment === "right" ? "right" : l.props.textAlignment === "justify" ? "distribute" : (() => {
                throw new T(l.props.textAlignment);
              })(),
              run: {
                bold: c || u,
                color: l.props.textColor === "default" || !l.props.textColor ? void 0 : (() => {
                  var _a2;
                  let e2 = (_a2 = t.options.colors[l.props.textColor]) == null ? void 0 : _a2.text;
                  if (e2) return e2.slice(1);
                })()
              }
            })]
          });
        })
      });
    })
  });
};
function x2(e, t) {
  return {
    shading: e.backgroundColor === "default" || !e.backgroundColor ? void 0 : {
      type: ShadingType.CLEAR,
      fill: (() => {
        var _a;
        let n = (_a = t[e.backgroundColor]) == null ? void 0 : _a.background;
        if (n) return n.slice(1);
      })()
    },
    run: e.textColor === "default" || !e.textColor ? void 0 : { color: (() => {
      var _a;
      let n = (_a = t[e.textColor]) == null ? void 0 : _a.text;
      if (n) return n.slice(1);
    })() },
    alignment: !e.textAlignment || e.textAlignment === "left" ? void 0 : e.textAlignment === "center" ? "center" : e.textAlignment === "right" ? "right" : e.textAlignment === "justify" ? "distribute" : (() => {
      throw new T(e.textAlignment);
    })()
  };
}
var S = {
  paragraph: (e, t) => new Paragraph({
    ...x2(e.props, t.options.colors),
    children: t.transformInlineContent(e.content)
  }),
  toggleListItem: (e, t) => new Paragraph({
    ...x2(e.props, t.options.colors),
    children: [new TextRun({ children: ["> "] }), ...t.transformInlineContent(e.content)]
  }),
  numberedListItem: (e, t, n) => new Paragraph({
    ...x2(e.props, t.options.colors),
    children: t.transformInlineContent(e.content),
    numbering: {
      reference: "blocknote-numbered-list",
      level: n
    }
  }),
  bulletListItem: (e, t, n) => new Paragraph({
    ...x2(e.props, t.options.colors),
    children: t.transformInlineContent(e.content),
    numbering: {
      reference: "blocknote-bullet-list",
      level: n
    }
  }),
  checkListItem: (e, t) => new Paragraph({
    ...x2(e.props, t.options.colors),
    children: [
      new CheckBox({ checked: e.props.checked }),
      new TextRun({ children: [" "] }),
      ...t.transformInlineContent(e.content)
    ]
  }),
  heading: (e, t) => new Paragraph({
    ...x2(e.props, t.options.colors),
    children: t.transformInlineContent(e.content),
    heading: `Heading${e.props.level}`
  }),
  quote: (e, t) => new Paragraph({
    style: "BlockQuote",
    ...x2(e.props, t.options.colors),
    children: t.transformInlineContent(e.content)
  }),
  audio: (e, t) => [C(e.props, "Open audio", t), ...w(e.props, t)],
  video: (e, t) => [C(e.props, "Open video", t), ...w(e.props, t)],
  file: (e, t) => [C(e.props, "Open file", t), ...w(e.props, t)],
  codeBlock: (e) => {
    var _a;
    return new Paragraph({
      style: "SourceCode",
      children: [...(((_a = e.content[0]) == null ? void 0 : _a.text) || "").split("\n").map((e2, t) => new TextRun({
        text: e2,
        break: +(t > 0)
      }))]
    });
  },
  pageBreak: () => new Paragraph({ children: [new PageBreak()] }),
  divider: () => new Paragraph({ border: { top: {
    color: "auto",
    space: 1,
    style: "single",
    size: 1
  } } }),
  column: (e, t, n, r, i) => new TableCell({
    width: {
      size: `${e.props.width * 100}%`,
      type: "pct"
    },
    children: (i || []).flatMap((e2) => Array.isArray(e2) ? e2 : [e2])
  }),
  columnList: (e, t, n, r, i) => new Table({
    layout: "autofit",
    borders: {
      bottom: { style: "nil" },
      top: { style: "nil" },
      left: { style: "nil" },
      right: { style: "nil" },
      insideHorizontal: { style: "nil" },
      insideVertical: { style: "nil" }
    },
    rows: [new TableRow({ children: i.map((e2, t2, n2) => {
      var _a;
      return new TableCell({
        width: {
          size: `${parseFloat(`${((_a = e2.options.width) == null ? void 0 : _a.size) || "100%"}`) / (n2.length * 100) * 100}%`,
          type: "pct"
        },
        children: e2.options.children
      });
    }) })]
  }),
  image: async (e, t) => {
    let n = await t.resolveFile(e.props.url), { width: r, height: i } = await y(n);
    return [new Paragraph({
      ...x2(e.props, t.options.colors),
      children: [new ImageRun({
        data: await n.arrayBuffer(),
        type: "gif",
        altText: e.props.caption ? {
          description: e.props.caption,
          name: e.props.caption,
          title: e.props.caption
        } : void 0,
        transformation: {
          width: e.props.previewWidth || r,
          height: (e.props.previewWidth || r) / r * i
        }
      })]
    }), ...w(e.props, t)];
  },
  table: (e, t) => b(e.content, t)
};
function C(e, t, n) {
  return new Paragraph({
    ...x2(e, n.options.colors),
    children: [new ExternalHyperlink({
      children: [new TextRun({
        text: e.name || t,
        style: "Hyperlink"
      })],
      link: e.url
    })]
  });
}
function w(e, t) {
  return e.caption ? [new Paragraph({
    ...x2(e, t.options.colors),
    children: [new TextRun({ text: e.caption })],
    style: "Caption"
  })] : [];
}
var T2 = {
  blockMapping: S,
  inlineContentMapping: {
    link: (e, t) => new ExternalHyperlink({
      children: e.content.map((e2) => t.transformStyledText(e2, true)),
      link: e.href
    }),
    text: (e, t) => t.transformStyledText(e)
  },
  styleMapping: {
    bold: (e) => e ? { bold: e } : {},
    italic: (e) => e ? { italics: e } : {},
    underline: (e) => e ? { underline: { type: "single" } } : {},
    strike: (e) => e ? { strike: e } : {},
    backgroundColor: (e, t) => {
      var _a;
      if (!e) return {};
      let n = (_a = t.options.colors[e]) == null ? void 0 : _a.background;
      return n ? { shading: {
        type: ShadingType.CLEAR,
        fill: n.slice(1)
      } } : {};
    },
    textColor: (e, t) => {
      var _a;
      if (!e) return {};
      let n = (_a = t.options.colors[e]) == null ? void 0 : _a.text;
      return n ? { color: n.slice(1) } : {};
    },
    code: (e) => e ? { style: "VerbatimChar" } : {}
  }
};
async function E(e) {
  return "https://corsproxy.api.blocknotejs.org/corsproxy/?url=" + encodeURIComponent(e);
}
async function D(e) {
  {
    let t = e.default;
    return await (await fetch(t)).arrayBuffer();
  }
}
var O = 16 * 0.75 * 1.5 * 20;
var k = class extends Ni {
  constructor(t, n, r) {
    let i = {
      colors: G,
      resolveFileUrl: E,
      ...r
    };
    super(t, n, i), this.schema = t, this.mappings = n;
  }
  transformStyledText(e, t) {
    let n = this.mapStyles(e.styles), r = Object.assign({}, ...n);
    return new TextRun({
      ...r,
      style: t ? "Hyperlink" : r.style,
      text: e.text
    });
  }
  async transformBlocks(e, t = 0) {
    let n = [];
    for (let r of e) {
      let e2 = await this.transformBlocks(r.children, t + 1);
      ["columnList", "column"].includes(r.type) || (e2 = e2.map((e3, t2) => (e3 instanceof Paragraph && !e3.properties.numberingReferences.length && e3.addRunToFront(new TextRun({ children: [new Tab()] })), e3)));
      let i = await this.mapBlock(r, t, 0, e2);
      ["columnList", "column"].includes(r.type) ? n.push(i) : Array.isArray(i) ? n.push(...i, ...e2) : n.push(i, ...e2);
    }
    return n;
  }
  async getFonts() {
    let e = await D(await import("./Inter_18pt-Regular-DVodnrPc-B3OQI7EJ.js")), t = await D(await import("./GeistMono-Regular-DC6WVheu-IIAIXCRS.js"));
    if (e instanceof ArrayBuffer || t instanceof ArrayBuffer) {
      let n = (await import("./buffer.js")).Buffer;
      e instanceof ArrayBuffer && (e = n.from(e)), t instanceof ArrayBuffer && (t = n.from(t));
    }
    return [{
      name: "Inter",
      data: e
    }, {
      name: "GeistMono",
      data: t
    }];
  }
  async createDefaultDocumentOptions(e) {
    let t = (await import("./styles-C7Ws_DIi-P7ZU4J4H.js")).default, n = e == null ? void 0 : e.trim();
    t = n ? t.replace(/(<w:lang\b[^>]*\bw:val=")([^"]+)("[^>]*\/>)/g, (e2, t2, r2, i) => `${t2}${n}${i}`) : t.replace(/\s*<w:lang\b[^>]*\/>/g, "");
    let r = ["•"];
    return {
      numbering: { config: [{
        reference: "blocknote-numbered-list",
        levels: Array.from({ length: 9 }, (e2, t2) => ({
          start: 1,
          level: t2,
          format: LevelFormat.DECIMAL,
          text: `%${t2 + 1}.`,
          alignment: AlignmentType.LEFT,
          style: { paragraph: { indent: {
            left: O * (t2 + 1),
            hanging: O
          } } }
        }))
      }, {
        reference: "blocknote-bullet-list",
        levels: Array.from({ length: 9 }, (e2, t2) => ({
          start: 1,
          level: t2,
          format: LevelFormat.BULLET,
          text: r[t2 % r.length],
          alignment: AlignmentType.LEFT,
          style: { paragraph: { indent: {
            left: O * (t2 + 1),
            hanging: O
          } } }
        }))
      }] },
      fonts: await this.getFonts(),
      defaultTabStop: 200,
      externalStyles: t
    };
  }
  async toBlob(e, t = {
    sectionOptions: {},
    documentOptions: {}
  }) {
    let n = await this.toDocxJsDocument(e, t), r = globalThis.Buffer;
    try {
      return globalThis.Buffer || (globalThis.Buffer = (await import("./buffer.js")).default.Buffer), Packer.toBlob(n);
    } finally {
      globalThis.Buffer = r;
    }
  }
  async toDocxJsDocument(e, t = {
    sectionOptions: {},
    documentOptions: {}
  }) {
    return new File({
      ...await this.createDefaultDocumentOptions(t.locale),
      ...t.documentOptions,
      sections: [{
        children: await this.transformBlocks(e),
        ...t.sectionOptions
      }]
    });
  }
};
export {
  k as DOCXExporter,
  T2 as docxDefaultSchemaMappings
};
//# sourceMappingURL=@blocknote_xl-docx-exporter.js.map
